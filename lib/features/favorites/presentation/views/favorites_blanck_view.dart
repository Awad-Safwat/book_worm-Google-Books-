import 'package:flutter/material.dart';

class FavoritesBlanckView extends StatelessWidget {
  const FavoritesBlanckView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(),
    );
  }
}
